1.单例模式作用：使得类的一个对象成为系统中唯一实例，并提供一个全局访问点
2.具体就是单例类中有一个静态的变量，私有化构造方法，提供唯一全局调用方法，并选择一个时机进行初始化
3.单例分饿汉与懒汉，饿汉就是服务启动直接加载（静态方法），懒汉就是用到再加载
4.在例子3中是保证线程安全了，但是因为上锁了，导致必须第一个线程用完下一个线程才能再用，影响效率。
例子4是对例子3的改进，有两次判断null，第一个判断是否已经初始化了单例，如果已经创建了，则不进行线程阻塞直接返回，
第二个判断是-在没有初始化的情况下，需要初始话，但是不是因为线程阻塞的原因，已经初始话了，如果已经初始话了，则返回，没有，则进行线程初始化返回